[project]
name = "globus-ink-26"
version = "0.1.0"
description = "Orbit-tracking replica of the Soviet Globus INK-25 navigation instrument"
readme = "README.md"
license = "GPL-3.0-or-later"
requires-python = ">=3.11"
dependencies = [
    "pyserial>=3.5",
    "skyfield>=1.54",
    "numpy>=1.24",
]
